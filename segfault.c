#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
	// with this command on the command prompt of linux
	// ./a.out > text.txt
	// this operator ">" tells the Operating system to connect the standard output of the program
	// to this filename(text.txt)
	printf("kaigama is a good boy\n");
	// while < tells the operating system to connect the standard inputof the program
	//to this filename(text.txt)
	//char file[100];
	//scanf("%109s", file);
	return (0);
}
